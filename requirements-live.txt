# OPTIONAL — install only on a GPU host to run LIVE benchmarks.
#   pip install -r requirements-live.txt
# These are intentionally version-banded (not fully pinned) because the
# correct build depends on your CUDA / Python toolchain.
torch>=2.3.0
transformers>=4.42.0
accelerate>=0.31.0
bitsandbytes>=0.43.0
autoawq>=0.2.5
gptqmodel>=0.1.0
